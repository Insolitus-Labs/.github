{
  "name": "my-v0-project",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "format": "prettier --write .",
    "test": "jest",
    "deploy": "gh-pages -d out",
    "clean": "rimraf out",
    "version": "npm version patch" // Added version script for versioning
  },
  "dependencies": {
    "next": "latest",
    "react": "^18",
    "react-dom": "^18",
    "axios": "^1.3.0",
    "zustand": "^4.4.0",
    "react-query": "^3.39.0"
  },
  "devDependencies": {
    "jest": "^27.0.0",
    "gh-pages": "^3.2.3",
    "rimraf": "latest",
    "prettier": "^2.3.2" // Added Prettier as a devDependency for code formatting
  }
}
